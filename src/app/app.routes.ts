import { Routes } from '@angular/router';
import { AlbumsPageComponent } from './albums/components/albums-page.component';

export const routes: Routes = [
  { path: '', component: AlbumsPageComponent },
];
